{
  "name": "scan-retrieval",
  "version": "1.0.0",
  "description": "this uses an express server to retrieve scanned patrons from a mongodb cluster on their cloud platform. the scanned patrons are 'cached' and deleted daily.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^3.4.0"
  }
}
